package transport;
public class Bus extends Transport {
    public Bus(String mark, String model, int year, String country, String colour, int maxSpeed) {
        super(mark, model, year, country, colour, maxSpeed);
    }
}
